# راهنمای نصب پروژه Arcbes روی هاست

## 🚀 مراحل نصب

### 1. آپلود فایل‌ها
- کل محتوای پوشه `arcbes-website` را روی هاست آپلود کنید
- اطمینان حاصل کنید که ساختار پوشه‌ها حفظ شود

### 2. ایجاد دیتابیس MySQL
1. وارد cPanel یا DirectAdmin شوید
2. MySQL Databases را باز کنید
3. یک دیتابیس جدید با نام دلخواه (مثلاً `arcbes_db`) ایجاد کنید
4. یک کاربر با دسترسی کامل به دیتابیس بسازید
5. کاربر را به دیتابیس متصل کنید

### 3. تنظیم فایل env.php
فایل `api/config/env.php` را ویرایش کنید:

```php
return [
    'DB_HOST' => 'localhost',  // ممکن است در هاست متفاوت باشد
    'DB_NAME' => 'نام_دیتابیس_شما',
    'DB_USER' => 'نام_کاربری_دیتابیس',
    'DB_PASS' => 'رمز_عبور_دیتابیس',
    
    // تنظیمات SMS
    'SMS_API_KEY' => 'کلید_API_کاوه‌نگار',
    'SMS_TEMPLATE' => 'arcbes-otp',
    
    // تنظیمات برنامه
    'APP_URL' => 'https://دامین_شما.ir',  // آدرس اصلی سایت
    'JWT_SECRET' => 'یک_رشته_تصادفی_طولانی_حداقل_64_کاراکتر',
    'APP_DEBUG' => false,  // در حالت production روی false قرار دهید
];
```

### 4. اجرای اسکریپت دیتابیس
اسکریپت `database.sql` را در phpMyAdmin یا با ابزارهای مشابه اجرا کنید.

### 5. تنظیمات پوشه uploads
- پوشه `uploads` باید دسترسی نوشتن (write permission) داشته باشد
- تنظیمات پیشنهادی: `755` یا `775`

### 6. تنظیمات وب سرور
#### برای Apache:
```
DocumentRoot /path/to/arcbes-website
DirectoryIndex index.html index.php
```

#### پیکربندی URL (مهم):
- مسیر اصلی سایت: `index.html`
- پنل مدیریت: `/admin/`
- API: `/api/`

### 7. ایمن‌سازی فایل‌ها
- فایل `env.php` را از دسترسی مستقیم محافظت کنید
- پوشه `api/config` را ایمن کنید

## 🔧 تنظیمات SMS
برای فعال‌سازی ارسال SMS:
1. در [پنل کاوه‌نگار](https://panel.kavenegar.com) ثبت‌نام کنید
2. API-KEY دریافت کنید
3. یک قالب lookup با نام `arcbes-otp` ایجاد کنید
4. متن قالب: `کد تایید شما %token%`
5. کلید API را در `env.php` وارد کنید

## 📂 ساختار پوشه‌ها
```
arcbes-website/
├── index.html              # صفحه اصلی
├── admin/                  # پنل مدیریت
├── api/                    # API بک‌اند
│   ├── config/
│   │   ├── db.php         # تنظیمات دیتابیس
│   │   └── env.php        # تنظیمات محیطی (ویرایش شود)
│   ├── src/               # کدهای PHP
│   └── .htaccess          # تنظیمات Apache
├── assets/                 # استایل و جاوااسکریپت
├── uploads/                # آپلود فایل‌ها
└── blog.html, contact.html # صفحات استاتیک
```

## 🧪 تست نصب
پس از نصب، این موارد را تست کنید:
1. باز شدن صفحه اصلی: `https://دامین_شما.ir/`
2. پنل مدیریت: `https://دامین_شما.ir/admin/`
3. API: `https://دامین_شما.ir/api/status` (باید JSON برگرداند)
4. فرم تماس: `https://دامین_شما.ir/contact.html`
5. لاگین ادمین: `https://دامین_شما.ir/login.html`

## 🛠 اطلاعات ورود پیش‌فرض
- آدرس پنل: `دامین_شما/admin/`
- شماره موبایل ادمین: `09120000000`
- کلمه عبور: `Admin123!`

## ⚠️ نکات امنیتی
1. `APP_DEBUG` را در محیط production روی `false` قرار دهید
2. `JWT_SECRET` را با یک رشته تصادفی قوی جایگزین کنید
3. دسترسی پوشه `uploads` را محدود کنید
4. به‌روزرسانی‌های امنیتی PHP را دنبال کنید

## 📞 پشتیبانی
در صورت نیاز به کمک:
- مستندات در فایل `README.md` در پوشه `api`
- بررسی لاگ‌ها در پوشه `api` (در صورت فعال بودن لاگینگ)