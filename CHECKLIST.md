# ✅ چک‌لیست آماده‌سازی برای هاست

## قبل از آپلود
- [ ] فایل‌های log حذف شده‌اند ✅
- [ ] فایل .gitignore ایجاد شده ✅
- [ ] فایل‌های اضافی و موقت پاک شده‌اند

## فایل‌های اصلی آماده‌سازی
- [ ] `DEPLOYMENT.md` - راهنمای نصب ✅
- [ ] `README.md` - مستندات اصلی ✅  
- [ ] `api/config/env.example.php` - نمونه تنظیمات ✅
- [ ] `database.sql` - ساختار دیتابیس ✅
- [ ] `.gitignore` - فایل‌های نادیده ✅

## پس از آپلود روی هاست
### مراحل ضروری:
1. **آپلود تمام فایل‌ها** به هاست (به جز فایل‌های .gitignore)
2. **ایجاد دیتابیس** MySQL در cPanel
3. **ویرایش `env.php`**:
   - کپی از `env.example.php` به `env.php`
   - وارد کردن اطلاعات دیتابیس هاست
   - تنظیم `APP_URL` به آدرس دامنه
   - تنظیم `JWT_SECRET` به یک رشته امن
   - تنظیم `APP_DEBUG` به `false`
4. **اجرای اسکریپت دیتابیس** (`database.sql`) در phpMyAdmin
5. **تنظیم مجوزهای پوشه‌ها**:
   - پوشه `uploads` → `755` یا `775`
   - پوشه `api/config` → `644` (فقط خواندن)
6. **تست سایت**:
   - صفحه اصلی: `https://دامین_شما.ir/`
   - پنل مدیریت: `https://دامین_شما.ir/admin/`
   - API وضعیت: `https://دامین_شما.ir/api/status`

### تنظیمات SMS (اختیاری اما توصیه‌شده):
1. ثبت‌نام در [پنل کاوه‌نگار](https://panel.kavenegar.com)
2. دریافت API-KEY
3. ایجاد قالب `arcbes-otp` با متن: `کد تایید شما %token%`
4. وارد کردن API-KEY در `env.php`

## 🔧 اطلاعات فنی پروژه
- **حجم کل**: ~0.43 مگابایت ✅
- **تعداد فایل‌ها**: 44 فایل ✅
- **پشتیبانی PHP**: 7.4+ ✅
- **پشتیبانی MySQL**: 5.7+ ✅
- **طراحی**: واکنشگرا (Responsive) ✅
- **امنیت**: JWT + Rate Limiting + CSRF ✅

## 🚨 نکات مهم
- فایل `env.php` را در گیت commit نکنید!
- پس از اولین لاگین، رمز ادمین را تغییر دهید
- در محیط production، `APP_DEBUG` باید `false` باشد
- پشتیبان‌گیری منظم از دیتابیس داشته باشید
- به‌روزرسانی‌های امنیتی PHP را پیگیری کنید

## 📞 تست نهایی
- [ ] صفحه اصلی بارگذاری می‌شود
- [ ] فرم‌ها کار می‌کنند (تماس، مشاوره)
- [ ] پنل مدیریت قابل دسترسی است
- [ ] API پاسخ می‌دهد
- [ ] آپلود تصاویر کار می‌کند
- [ ] SMS ارسال می‌شود (اگر تنظیم شده)

**پروژه آماده آپلود است!** 🎉